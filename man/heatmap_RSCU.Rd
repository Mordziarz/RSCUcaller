% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap_RSCU.R
\name{heatmap_RSCU}
\alias{heatmap_RSCU}
\title{Visualize RSCU Patterns Through Heatmaps and Dendrograms}
\usage{
heatmap_RSCU(heatmap_color = "", get_RSCU_out = get_RSCU_out, select = "")
}
\arguments{
\item{heatmap_color}{Color scheme for heatmap visualization. One of:
\itemize{
  \item "red_green" - Red (low) to Green (high)
  \item "green_red" - Green (low) to Red (high)
  \item "blue_green" - Blue (low) to Green (high)
  \item "green_blue" - Green (low) to Blue (high)
  \item "blue_red" - Blue (low) to Red (high)
  \item "red_blue" - Red (low) to Blue (high)
}}

\item{get_RSCU_out}{A data frame generated by \code{\link{get_RSCU}} containing RSCU values}

\item{select}{Type of visualization to return: 
\itemize{
  \item "heatmap" - Colored matrix with hierarchical clustering
  \item "dendrogram" - Sample clustering tree with branch lengths
}}
}
\value{
Depending on \code{select} parameter:
\itemize{
  \item For "heatmap": Returns a \code{Heatmap} object from \code{ComplexHeatmap}
  \item For "dendrogram": Returns a \code{ggtree} object and writes Newick file
}
}
\description{
Generates two complementary visualizations of Relative Synonymous Codon Usage (RSCU) data:
1) A clustered heatmap showing codon usage patterns across samples
2) A hierarchical clustering dendrogram of samples
}
\details{
The heatmap visualization uses hierarchical clustering (Euclidean distance, complete linkage) 
to group samples by similar codon usage patterns. The dendrogram provides a phylogenetic-style
representation of sample relationships. Both visualizations use the same clustering algorithm.
}
\examples{
withr::with_tempdir({data("prepared_fasta", package = "RSCUcaller")
rscu_data <- get_RSCU(prepared_fasta)

# Generate heatmap
heatmap_RSCU(get_RSCU_out = rscu_data, 
             heatmap_color = "red_blue",
             select = "heatmap")
             
# Generate dendrogram
heatmap_RSCU(get_RSCU_out = rscu_data,
             select = "dendrogram")

}})
\references{
Gu, Z. (2016) Complex heatmaps reveal patterns and correlations in multidimensional 
genomic data. Bioinformatics, 32(18), 2847-2849.
}
\seealso{
Related visualization functions:
\code{\link{histogram_RSCU}}, \code{\link{PCA_RSCU}}
}
